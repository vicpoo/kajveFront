// core/api-config.ts - URLs base centralizadas del gateway
export const GATEWAY_URL = 'https://gateway.dnc-ed-denz.shop';
export const WEB_API_BASE_URL = `${GATEWAY_URL}/web/api/v1`;
export const WEB_ROOT_URL = `${GATEWAY_URL}/web`; // para /health, que no lleva /api/v1
export const PAGOS_BASE_URL = `${GATEWAY_URL}/pagos`;
